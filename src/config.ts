// iCloud link to the "Sign PDF" iOS Shortcut.
//
// To generate it: build the shortcut on an iPhone (see README → iOS Shortcut),
// then Shortcuts app → the shortcut → Share → Copy iCloud Link, and paste the
// resulting https://www.icloud.com/shortcuts/<id> URL here.
//
// When set, the landing page shows a one-tap "Add the iOS shortcut" link on iOS.
export const IOS_SHORTCUT_URL = "";
